apiVersion: v1
kind: Service
metadata:
  name: {{ include "booth-storage.fullname" . }}
  labels:
    {{- include "booth-storage.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "booth-storage.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
